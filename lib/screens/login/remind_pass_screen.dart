import 'package:flutter/material.dart';

class RemindPassScreen extends StatelessWidget {
  const RemindPassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        margin: const EdgeInsets.symmetric(horizontal: 10),
      ),
    );
  }
}
